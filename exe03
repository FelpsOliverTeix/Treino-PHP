/* Crie um algoritmo que solicite um número, e faça o calculo fatorial  e exiba o resultado*/
<?php
    $num = 5;

  if($num > 0){
    $valor = $num;
    for($i = ($valor - 1); $i > 0; $i--){
      $valor = $valor * $i;
    }
  }else{
    $valor = 0;
  }

  echo "!{$num} = {$valor}";
?>
